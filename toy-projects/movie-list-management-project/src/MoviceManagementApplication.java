import movie.MovieMenu;

public class MoviceManagementApplication {
    public static void main(String[] args) {
        MovieMenu movieMenu = new MovieMenu();
        movieMenu.menu();
    }
}
